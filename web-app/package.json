{
  "name": "web-app",
  "version": "1.0.0",
  "description": "A web app that collects a user's image prompt and text prompt, sends them to OpenAI, and displays the output.",
  "main": "src/index.js",
  "scripts": {
    "start": "node server/server.js",
    "client": "react-scripts start",
    "server": "nodemon server/server.js",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "openai": "^3.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "proxy": "http://localhost:5000",
  "author": "",
  "license": "ISC"
}